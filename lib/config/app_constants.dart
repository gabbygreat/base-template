import '../utils/utils.dart';

class AppConstant {
  static const String baseUrl = 'https://api1.kouakoudomagni.com';
  static const String fontFamily = 'Inter';

  static const Velocity velocity = Velocity(pixelsPerSecond: Offset(20, 0));
  static String? oldPath;
}
