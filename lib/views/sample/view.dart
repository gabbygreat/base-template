
part of 'controller.dart';

class SampleView extends StatelessView<SampleScreen, SampleController> {
  const SampleView(super.state, {super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
